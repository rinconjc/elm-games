module Utils.Triplet exposing (findMatches)

import Array exposing (Array)
import Utils.Grid as Grid


findMatches : Grid.Grid -> List ( Int, Int )
findMatches grid =
    -- TODO: Implement the actual sum-swap triplet matching logic
    -- This should return positions of all tiles that form valid triplets
    -- where a + b = c (in any order)
    []
